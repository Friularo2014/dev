class PrimeGenerator:
    def __init__(self):
        self.currentNum = 1

    def currentNumIsPrime():
        if (): return False

    def getNextPrime(self):
        self.currentNum += 1
        if (not currentNumIsPrime()):
            self.currentNum += 1
        return self.currentNum

primes_num = 4
vLen = primes_num + 1

v = []

primeGenerator = PrimeGenerator()

for i in range(vLen):
    print(i)
    v.append(primeGenerator.getNextPrime())

print(v)
